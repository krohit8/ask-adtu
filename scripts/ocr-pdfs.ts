// Replaced by ocr-pdfs.mts.
//
// This script must run as an ES module (pdf-to-img -> pdfjs-dist uses top-level
// await, which cannot compile to CommonJS - the format tsx uses for `.ts` files
// in this project). The working version lives in ocr-pdfs.mts and is what
// `npm run ocr` executes. You can safely delete this file.
export {};
